# Copy to .env and provide your Google OAuth 2.0 Web client ID
# Create OAuth credentials at https://console.cloud.google.com/apis/credentials
# Authorized JavaScript origins must include your dev/host URLs (e.g., http://localhost:5173)
# For PWA installed mode, include the origin where it will be hosted.
VITE_GOOGLE_CLIENT_ID=
